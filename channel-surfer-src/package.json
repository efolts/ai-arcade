{
  "name": "channel-surfer",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --base ./ --host 127.0.0.1 --port 4173",
    "test": "node --test src/sim.test.js"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
